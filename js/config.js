/* ============================================================
   SITE CONFIGURATION — the owner edits ONLY this file.
   ============================================================ */
window.SITE_CONFIG = {
  // 1) LEAD FORM. Free option: create an account at https://web3forms.com, paste the Access Key here.
  //    Every "Request a callback" submission is emailed to the address on that account.
  //    Leave blank and the form falls back to opening the visitor's email app with the details pre-filled.
  formAccessKey: "",
  leadEmail: "info@epicmedianetworks.com",

  // 2) SCHEDULE A CALL. Paste a Calendly / Google booking link. Leave blank to fall back to the phone number.
  schedulerUrl: "",

  phone: "(602) 410-7141",
  phoneHref: "tel:+16024107141"
};
